describe('test', () => {
  test('should work', () => {
    expect(true).toEqual(true);
  });

  test('should work', () => {
    expect(true).toEqual(true);
  });

  test('should work 2', () => {
    expect(true).toEqual(true);
  });
})